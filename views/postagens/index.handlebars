<div class="card">
    <div class="card-body">
        <h1>{{postagens.titulo}}</h1>
        <small>Data da postagem: {{postagens.data}}</small>
        <hr>
        {{postagens.conteudo}}
    </div>
</div>